import Home from "../Home";
import Login from "../Login";

export const login = {
    name: 'Login',
    pathname: '/login',
    component: Login
};

export const home = {
    name: 'Home',
    pathname: '/',
    component: Home
}